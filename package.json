{
  "name": "websockets-rabbitmq-benchmark",
  "version": "0.0.1",
  "type": "module",
  "dependencies": {
    "@stomp/stompjs": "^7.0.0",
    "dotenv": "^16.3.1",
    "express": "^4.19.2",
    "mqtt": "^5.0.5",
    "ws": "^8.14.2"
  },
  "scripts": {
    "stomp-connections": "cd web_stomp && NODE_ENV=connections node index.js",
    "mqtt-messages": "cd web_mqtt && NODE_ENV=messages node index.js",
    "amqp-connections": "cd web_amqp && NODE_ENV=connections node index.js",
    "stomp-messages": "cd web_stomp && NODE_ENV=messages node index.js",
    "mqtt-connections": "cd web_mqtt && NODE_ENV=connections node index.js",
    "amqp-messages": "cd web_amqp && NODE_ENV=messages node index.js"
  }
}
